## Packages
(none needed)

## Notes
Uses existing shadcn/ui components already in repo (Button, Card, Dialog, Accordion, Tabs, Badge, Toast).
API: POST /api/analyze (no auth), expects { message: string } and returns AnalyzeResponse.
No streaming. No file uploads. No images required.
